Io 就是输入和输出 


高级I/O 
ANSI C提供的标准io库 成为高级IO,通常也称为带缓冲的IO

低级I/O
通常被称为不带缓冲 

一个进程启动的时候,默认打开三个文件，标准输入，标准输出，标准错误，
对应文件描述符是0(STDIN_FILEN0),1(STDOUT_FILEN0),2(STDERR_FILENO)，这些常量定义在unistd.h文件中  

fileno: 将文件指针转换成文件描述符
fopen：将文件描述符转换为文件指针